#include "main.h"
/**
 * _abs - Entry Point
 *@n: the number which we will take its abs value
 * Description: Get absolute of a number
 * Return: n (If the number is positive)
 *-n (If the number is negative)
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	return (-n);
}
