#include "main.h"
/**
 * _islower - Entry Point
 *@e: the character to check
 * Description: Checks whether a character is in lower case
 * Return: Always 0 (Success)
 */
int _islower(int e)
{
	return (e >= 'a' && e <= 'z');
}
