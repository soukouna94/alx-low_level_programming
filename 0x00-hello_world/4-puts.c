#include<stdio.h>

/**
 * main - Prints a statement
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char stringToPrint[] = "\"Programming is like building a multilingual puzzle";

	puts(stringToPrint);

	return (0);
}
